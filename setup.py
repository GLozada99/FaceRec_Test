from setuptools import setup, find_packages
# Package (minimal) configuration
setup(
    name="AccessControl",
    version="1.0.0",
    description="Smart Automation Work Reception",
    packages=find_packages(),  # __init__.py folders search
    #install_requires=requirements
)