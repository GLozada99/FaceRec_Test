Usar python 3.8

Para utilizar el proyecto y acceder a los módulos propios creados de manera adecuada, ejecutar en el directorio raiz:

1. python -m venv venv
2. source venv/bin/activate
3. pip install -r AccessControl/requirements.txt
4. pip install -e .

CMake debe estar instalado para hacerle el build a dlib, que es un módulo que utiliza face_recognition

La base de datos en Postgres debe tener un datestyle DMY. Ej:
SET datestyle = GERMAN, DMY;

